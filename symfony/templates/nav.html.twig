<ul class="nav">
	<li class="nav-item">
		<a class="nav-link" href="{{ path('tasks_list') }}">Home</a>
	</li>
	<li class="nav-item">
		<a class="nav-link" href="{{ path('tasks_create') }}">Create task</a>
	</li>
	<li class="nav-item ml-md-auto">
		{% if is_granted('ROLE_ADMIN') %}
			<a class="nav-link text-right" href="{{ path('app_logout') }}">Logout</a>
		{% else %}
			<a class="nav-link text-right" href="{{ path('app_login') }}">Login</a>
		{% endif %}
	</li>
</ul>